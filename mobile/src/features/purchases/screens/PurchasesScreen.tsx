import { Text } from 'react-native';
import { Screen } from '../../../components/Screen';

export function PurchasesScreen() {
  return (
    <Screen>
      <Text style={{ fontSize: 20, fontWeight: '700' }}>Purchases</Text>
    </Screen>
  );
}

