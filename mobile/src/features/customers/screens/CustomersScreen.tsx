import { Text } from 'react-native';
import { Screen } from '../../../components/Screen';

export function CustomersScreen() {
  return (
    <Screen>
      <Text style={{ fontSize: 20, fontWeight: '700' }}>Customers</Text>
    </Screen>
  );
}

